import type { Metadata } from 'next'
import AdminView from './admin-view'

export const metadata: Metadata = { title: 'Admin — Manju Trade AI Pro' }
export default function AdminPage() { return <AdminView /> }
