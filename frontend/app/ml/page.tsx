import type { Metadata } from 'next'
import MLView from './ml-view'

export const metadata: Metadata = { title: 'ML Signals — Manju Trade AI Pro' }
export default function MLPage() { return <MLView /> }
