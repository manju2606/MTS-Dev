import type { Metadata } from 'next'
import DiscoveryView from './discovery-view'

export const metadata: Metadata = { title: 'Stock Discovery — Manju Trade AI Pro' }

export default function DiscoveryPage() {
  return <DiscoveryView />
}
