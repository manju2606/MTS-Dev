import type { Metadata } from 'next'
import StrategyLabView from './strategy-lab-view'

export const metadata: Metadata = { title: 'AI Strategy Lab — Manju Trade AI Pro' }
export default function StrategyLabPage() { return <StrategyLabView /> }
