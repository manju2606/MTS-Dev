import type { Metadata } from 'next'
import BrokerView from './broker-view'

export const metadata: Metadata = { title: 'Broker — Manju Trade AI Pro' }
export default function BrokerPage() { return <BrokerView /> }
