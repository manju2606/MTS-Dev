apiVersion: v1
kind: Service
metadata:
  name: kube-state-metrics
  namespace: mts
spec:
  type: NodePort
  selector:
    app: kube-state-metrics
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080
      nodePort: 30080
